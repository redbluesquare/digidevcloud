// Register `default` component, along with its associated controller and template
angular.module('ddcApp').
    component('ddcApp', {
    templateUrl:'../app/default/default.template.html'//,
    
  });